INFORMATION RETRIEVAL PROJECT README.txt file

There are 5 files were zipped
	1.Entertainment.csv
	2.trie
	3.preprocessing.py
	4.retrieve.ipynb
	5.Documentation.

*we will do preprocessing to eliminate stopwords  so from nltk module stopwords should be imported'from nltk.corpus import stopwords'

Here,Inverted Index is created .
	*To avoid that local variable 'invertedIndex ={}' is stored after running it,it is stored using pickle library in python
	*Now the stored local variable can be loaded using pickle library
In report it explains how we used algorithms for achieving the tasks..
